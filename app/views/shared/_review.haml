%h3.in-gold-500.mb-25= t('reviews') + " (#{@reviews.count})"
- @reviews.each do |review|
  .general-message-wrap.divider-lg
    .clearfix
      = review.user_avatar
      .ml-70
        %div
          .general-message-date.hidden-xs= review.created_at
          .max-240
            .general-message-verified= t('verified') if review.verified?
            .general-message-name= review.user_name
        = review.book_rate
        %p= review.content
