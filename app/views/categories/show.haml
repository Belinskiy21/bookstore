%h2= @category.name
- @books.each_slice(4) do |book|
  .row
    - book.each do |book|
      .col-xs-6.col-sm-3
        .general-thumb-wrap
          .thumbnail.general-thumbnail
            = image_tag book.images.pluck(:file) , alt: 'design-book', class: 'img-shadow general-thumbnail-img'
            .thumb-hover
              .general-thumb-link-wrap
                %a.thumb-hover-link{href: book_path(:id)}
                  %i.fa.fa-eye.thumb-icon{'aria-hidden': "true"}
                %a.thumb-hover-link{href: "#"}
                  %i.fa.fa-shopping-cart.thumb-icon
        .general-thumb-info
          %p.title=book.title
          %p.font-16.in-gold-500=book.price
          %span.divider-sm
          %p.lead.small=authors_name_select(book)
